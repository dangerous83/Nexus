{
  "name": "nexus-infrastructure",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Nexus — White-label Crypto Exchange Platform. Marketing site.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:docs": "vite build && rm -rf docs && cp -r dist docs && touch docs/.nojekyll",
    "preview": "vite preview",
    "assets": "node scripts/fetch-assets.mjs",
    "optimize": "node scripts/optimize-assets.mjs",
    "favicons": "node scripts/gen-favicons.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.60.0",
    "sharp": "^0.33.5",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "@fontsource-variable/jetbrains-mono": "^5.1.1",
    "@fontsource-variable/space-grotesk": "^5.1.0",
    "gsap": "^3.12.5",
    "lenis": "^1.1.19"
  }
}
